"use client";

/**
 * useActiveBusinessId – stub hook.
 * Returns a simple business profile. Replace with real implementation.
 */
export function useActiveBusinessId() {
    return {
        businessId: null as string | null,
        businessProfile: {
            id: null as string | null,
            name: "Mi App",
            logoDataUrl: null as string | null,
        },
    };
}
